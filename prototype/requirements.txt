# RETICLE prototype — Python dependencies
# (the web server + external-source clients use only the stdlib otherwise)
numpy>=1.24
pandas>=2.0
scipy>=1.10
requests>=2.28
psycopg2-binary>=2.9      # only needed to use the AWS RDS (PostgreSQL) backend
